name: Export

on:
  push:
  pull_request:

jobs:
  export-gltf:
    name: Export gltf
    runs-on: ubuntu-24.04
    container:
      image: linuxserver/blender:5.1.2

    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: Generate gltf
      run: scripts/export-all.py

    - name: Upload gltf
      uses: actions/upload-artifact@v7
      with:
        name: gltf
        path: |
          *.glb
          *.png
          index.json

    - name: Publish gltf
      uses: softprops/action-gh-release@v3
      if: github.ref == 'refs/heads/master'
      with:
        tag_name: "environments-${{ github.sha }}"
        generate_release_notes: true
        draft: false
        prerelease: false
        fail_on_unmatched_files: true
        files: |
          *.glb
          *.png
          index.json
